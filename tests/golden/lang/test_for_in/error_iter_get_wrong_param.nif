class Seq {
    fn iter_len() -> u64 {
        return 0u;
    }

    fn iter_get(index: u64) -> i64 {
        return (i64)index;
    }
}

fn main() -> unit {
    var seq: Seq = Seq();
    for value in seq {
        return;
    }
    return;
}