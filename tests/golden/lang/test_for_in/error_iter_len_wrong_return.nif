class Seq {
    fn iter_len() -> i64 {
        return 0;
    }

    fn iter_get(index: i64) -> i64 {
        return index;
    }
}

fn main() -> unit {
    var seq: Seq = Seq();
    for value in seq {
        return;
    }
    return;
}