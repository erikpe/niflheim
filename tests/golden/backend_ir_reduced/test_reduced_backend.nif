fn inc(value: i64) -> i64 {
    return value + 1;
}

fn drive(limit: i64) -> i64 {
    var total: i64 = 0;
    while total < limit {
        total = inc(total);
    }
    if total == limit {
        return 0;
    }
    return 1;
}

fn main() -> i64 {
    return drive(4);
}
