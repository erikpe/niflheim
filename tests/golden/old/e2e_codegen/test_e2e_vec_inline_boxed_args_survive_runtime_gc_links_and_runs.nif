import std.box;
import std.vec;

fn main() -> i64 {
    var max: i64 = 2000;
    var flags: Vec = Vec.new();
    var i: i64 = 0;

    while i <= max {
        flags.push(BoxBool(true));
        i = i + 1;
    }

    flags.index_set(0, BoxBool(false));
    flags.index_set(1, BoxBool(false));

    var p: i64 = 2;
    while p <= max / p {
        if ((BoxBool)flags.index_get(p)).val {
            var m: i64 = p * p;
            while m <= max {
                flags.index_set(m, BoxBool(false));
                m = m + p;
            }
        }
        p = p + 1;
    }

    var primes: Vec = Vec.new();
    var n: i64 = 2;
    while n <= max {
        if ((BoxBool)flags.index_get(n)).val {
            primes.push(BoxI64(n));
        }
        n = n + 1;
    }

    if primes.len() != 303u {
        return 1;
    }
    return 0;
}
