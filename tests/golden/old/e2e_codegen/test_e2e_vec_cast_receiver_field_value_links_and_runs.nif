import std.box;
import std.vec;

fn main() -> i64 {
    var v: Vec = Vec.new();
    v.push(BoxI64(41));
    v.push(BoxI64(1));
    return ((BoxI64)v.index_get(0)).val + ((BoxI64)v.index_get(1)).val;
}
