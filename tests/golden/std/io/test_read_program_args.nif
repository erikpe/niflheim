import std.io;
import std.str;

fn main() -> i64 {
    var args: Str[] = read_program_args();
    println_u64(args.len());

    for arg in args[1:] {
        print("|");
        print((Str)arg);
        println("|");
    }

    return 0;
}
