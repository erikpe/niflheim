import a.b;
export import std.io;

class Point {
    x: i64;
    y: i64;

    fn move(dx: i64, dy: i64) -> unit {
        var nx: i64 = dx + 1;
        if nx > 0 {
            return;
        } else {
            return;
        }
    }
}

export fn main() -> unit {
    var i: i64 = 0;
    while i < 3 {
        i = i + 1;
    }
    return;
}
